service:
  http:
    port: :8080

auth:
  jwt:
    expire: 86400
    cookie:
      name: jwt-token
      maxAge: 86400
      secure: true
      httpOnly: true

mongo:
  driver: mongo

postgres:
  maxOpenConns: 60
  connMaxLifetime: 120
  maxIdleConns: 30
  connMaxIdleTime: 20
  driver: postgres

